
# Dragon's Share Quick Start

To run Dragon's Share on localhost:5001:

1. Do a `pip install -r requirements.txt` in the desired environment
2. Run the `main_desktop.py` module
3. Access http://localhost:5001 from your browser
